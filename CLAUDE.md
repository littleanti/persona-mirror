# CLAUDE.md

## 그라운드 룰 1 — 사고 절차 (필수)

결론을 내기 전에 반드시 아래 3단을 거친다. 원인 진단, 설계 선택, 수치 판단 모두에 적용된다.

1. **1차 사고** — 먼저 근거를 모아 가설과 결론을 세운다.
2. **비판적 재사고** — 그 결론을 *공격한다.* "이게 틀렸다면 무엇 때문인가", "이 근거가
   다른 것도 설명하지 않는가", "반증할 데이터는 무엇인가", "부작용이 나타날 조건은 무엇인가"를
   따진다. 가능하면 반증을 **실제로 측정하거나 재생해서** 확인한다.
3. **종합** — 1차와 2차를 합쳐 최종 결론을 낸다. 2차에서 살아남지 못한 부분은 버리고,
   살아남은 부분은 왜 살아남았는지를 근거와 함께 남긴다. 불확실하게 남은 것은 숨기지 말고
   "미확정" 으로 명시한다.

1차 결론을 그대로 답으로 내지 않는다. 2차에서 결론이 뒤집히면 뒤집힌 대로 보고한다.

> 이 절차가 필요한 이유는 실제 사례에서 나왔다 (2026-08-15 저속 검출 조사).
> 그럴듯했던 1차 가설 — 축 불균형, DIR_FLIP 폭주, `lock_revs` 변경이 만든 회귀 — 이
> **셋 다 실측 재생으로 반증됐다.** 또 획득 지연을 줄이려던 두 후보안은 84 rpm 에서
> 케이던스를 절반으로 무너뜨렸다. 1차 사고만으로 커밋했다면 전부 잘못된 수정이었다.

## 그라운드 룰 2 — 코드 수정 워크플로 (필수)

모든 **코드 추가/수정** 작업은 **코드를 건드리기 전에 먼저 문서를 업데이트**한다.

- 적용 대상: 기능 추가(feat)·버그 수정(fix)·동작 변경·리팩터링 등 **소스 코드가 바뀌는 모든 명령**.
- 순서는 항상 다음 5단계를 따른다:
  1. **① 문서 업데이트** — 변경 이력에 **착수 예정 항목**을 먼저 적는다(상태: `진행중`).
  2. **② 구현** — 코드를 작성/수정한다.
  3. **③ 검증(필요 시)** — typecheck/test/lint 등으로 동작을 확인한다. **항상 돌리지 않는다** — 아래 "검증 정책" 기준에 해당할 때만 실행한다.
  4. **④ 문서에 완료 표시** — 항목 상태를 `완료`로 바꾸고, 실제 변경 파일·결과를 정정 반영한다. 단 **검증 대상인데 아직 통과 못 한 항목은 `완료`로 표시하지 않는다**.
  5. **⑤ 커밋·푸시** — 위가 모두 끝난 뒤에만 커밋·푸시한다.

### 검증 정책 (토큰 낭비 방지)
- **매 변경마다 검증하지 않는다.** 다음 중 하나에 해당할 때만 ③을 실행한다:
  - **주요 마일스톤** 종료 시(예: PLAN의 Phase/하위 단계 P1-x 완료, 여러 변경의 묶음 마무리).
  - **커밋·푸시 직전**(⑤ 직전에 한 번 묶어서 검증).
  - 로직·계약·자료구조가 바뀌어 **회귀 위험이 실질적인** 변경.
  - 사용자가 **명시적으로 검증을 요청**한 경우.
- **검증을 건너뛰어도 되는 경우**(미검증 상태로 진행 가능):
  - 문서/주석/포매팅 등 동작이 바뀌지 않는 변경.
  - 탐색·실험적 중간 편집, 곧바로 추가 수정이 이어지는 작업.
- 검증을 건너뛴 항목은 changelog 상태를 `진행중`으로 두거나 `완료(미검증)`으로 명시하고, **다음 마일스톤/커밋 직전 검증에서 일괄 확인**한다.
- 검증을 실제로 돌렸다면 실패 시 항목은 `진행중`으로 두고 ②~③을 반복한다. **검증 결과(통과/실패)는 사실대로 기록**하고, 돌리지 않았으면 "검증 비대상/미실행"으로 적는다.
- 기본 문서는 [`docs/LOG.md`](./docs/LOG.md)의 변경 이력(Changelog)이다.
  최신 항목을 맨 위에 두고, 태그(`[feat]`/`[fix]`/`[test]`/`[docs]`/`[chore]`)와
  변경 파일 경로, 상태(`진행중`/`완료`)를 함께 적는다. 날짜는 절대 날짜로 기록한다.
- 스펙·계약·동작이 바뀌면 관련 상위 문서(`PRD.md`/`TRD.md`/`PLAN.md`/`DESIGN.md`)의
  해당 서술도 함께 갱신하고, 옛 서술이 새 동작과 모순되면 정정한다.
- 사소한 오타/포매팅 등 동작이 전혀 바뀌지 않는 변경은 이 규칙에서 제외한다.

## 참고
- 문서 인덱스/구조: `docs/` (PRD·TRD·PLAN·DESIGN·LOG).
